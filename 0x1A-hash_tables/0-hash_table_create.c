#include "hash_tabls.h"
/**
 *hash_table_create - creates a hash table
 *
 *@size: the size of the array
 *@Return: a pointer to a hash_table_t or NULL if it fail
 *
 *
 */
hash_table_t *hash_table_create(unsigned long int size)
{
}
